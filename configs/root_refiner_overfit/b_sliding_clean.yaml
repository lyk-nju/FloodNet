# B: sliding-window diagnostic. No path augmentation.
# Tests whether current_motion/history conditioning and nonzero anchors fit.

base_config: base.yaml

exp_name: root_refiner_overfit_b_sliding_clean
save_dir: ./outputs/root_refiner_overfit/b_sliding_clean

fixed_overfit:
  mode_policy: sliding
  force_no_path_aug: true
