# C: full + sliding mixed diagnostic. No path augmentation.
# Tests whether the shared model can fit both anchor regimes at once.

base_config: base.yaml

exp_name: root_refiner_overfit_c_mixed_clean
save_dir: ./outputs/root_refiner_overfit/c_mixed_clean

fixed_overfit:
  mode_policy: mixed
  force_no_path_aug: true
